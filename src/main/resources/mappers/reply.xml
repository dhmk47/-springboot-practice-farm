<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.springboot.farm.springbootpractice.domain.reply.ReplyRepository">
	<insert id="save" parameterType="com.springboot.farm.springbootpractice.domain.entity.Reply">
		insert into
			reply_mst
		values(
			0,
			#{board_code},
			#{user_code},
			#{reply},
			now(),
			now()
		)
	</insert>
	
	<select id="getReplyListByBoardCode" parameterType="Integer" resultType="com.springboot.farm.springbootpractice.domain.entity.Reply">
		SELECT
			rm.reply_code,
			rm.reply,
			rm.create_date,
			rm.update_date,
			
			um.name,
			um.roles
			
		FROM
			reply_mst rm
			left outer join user_mst um ON (um.usercode = rm.user_code)
		WHERE
			board_code = #{board_code}
	</select>
</mapper>